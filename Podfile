platform :ios, '15.0'

target 'Chatty' do
  use_frameworks!
  use_modular_headers!
  

  # Pods for Chatty
  # Commenting out Firebase pods to avoid conflicts with Swift Package Manager
  # pod 'Firebase/Auth'
  # pod 'Firebase/Firestore'
  # pod 'Firebase/Core'

end
